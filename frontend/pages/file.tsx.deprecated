import dynamic from "next/dynamic";

const GetUploadedFiles = dynamic(
  () => import("../components/GetUploadedFiles")
);

const file = () => {
  return (
    <div>
      <GetUploadedFiles />
      <div></div>
    </div>
  );
};

export default file;
