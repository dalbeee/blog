{
  "name": "@blog/core",
  "private": true,
  "version": "0.4.0",
  "description": "Common package for MonoRepo",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "rimraf": "rm -rf dist tsconfig.tsbuildinfo",
    "build:watch": "yarn rimraf && tsc -w",
    "build": "yarn rimraf && tsc --build"
  },
  "devDependencies": {
    "@types/jest": "^27.0.2",
    "jest": "^27.3.1",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "@babel/runtime": "^7.15.4",
    "class-validator": "^0.13.1"
  }
}
